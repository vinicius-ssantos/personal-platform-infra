apiVersion: v1
kind: Service
metadata:
  name: github-unified-mcp-bff
  namespace: bff
spec:
  selector:
    app: github-unified-mcp-bff
  ports:
    - name: http
      port: 8000
      targetPort: 8000
