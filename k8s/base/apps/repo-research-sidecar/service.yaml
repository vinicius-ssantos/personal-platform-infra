apiVersion: v1
kind: Service
metadata:
  name: repo-research-sidecar
  namespace: mcp
spec:
  selector:
    app: repo-research-sidecar
  ports:
    - name: http
      port: 8081
      targetPort: 8081
