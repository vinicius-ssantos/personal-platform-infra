resources:
  - ../../base
  - loki-pvc.yaml

patches:
  - path: loki-volume-patch.yaml

# VPS overlay keeps workloads sleeping by default with replicas=0.
# Add ingress, secrets and resource patches incrementally.
